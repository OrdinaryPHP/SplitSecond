name: "Ordinary PHP Code Quality"
on:
  push:
    branches:
      - dev
    tags:
      - '**'
permissions:
  packages: read
jobs:
  code-quality:
    strategy:
      matrix:
        php-version: [ "8.2", "8.3" ]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@main
      - uses: ordinaryphp/code-quality-action@initial
        with:
          version: branch-add-entrypoint
          phpVersion: ${{ matrix.php-version }}
